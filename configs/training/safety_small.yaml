dry_run: false
train_manifest: /data/nuplan/processed/safety_planner/v1/safety/train-manifest.jsonl
output_dir: /home/zhou/SafetyConsistentPlanner/outputs/checkpoints/safety_small
model:
  d_scene: 256
  d_entity: 64
  hidden_dim: 256
  safety_hidden_dim: 128
  num_candidates: 4
  num_future: 8
training:
  epochs: 5
  batch_size: 32
  learning_rate: 0.0001
  weight_decay: 0.00001
  gradient_clip_norm: 1.0
  seed: 42
  num_workers: 0
  device: cuda
